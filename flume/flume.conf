# ======================
# AGENT
# ======================
agent.sources = file-source
agent.channels = mem-channel
agent.sinks = hdfs-sink

# ======================
# SOURCE: SPOOLDIR
# ======================
agent.sources.file-source.type = spooldir
agent.sources.file-source.spoolDir = /data/flume-input
agent.sources.file-source.fileHeader = true

# ======================
# CHANNEL
# ======================
agent.channels.mem-channel.type = memory
agent.channels.mem-channel.capacity = 1000
agent.channels.mem-channel.transactionCapacity = 100

# ======================
# SINK: HDFS
# ======================
agent.sinks.hdfs-sink.type = hdfs
agent.sinks.hdfs-sink.hdfs.path = hdfs://namenode:9000/user/root/hospital_logs
agent.sinks.hdfs-sink.hdfs.filePrefix = hospital
agent.sinks.hdfs-sink.hdfs.fileType = DataStream
agent.sinks.hdfs-sink.hdfs.writeFormat = Text

agent.sinks.hdfs-sink.hdfs.rollInterval = 30
agent.sinks.hdfs-sink.hdfs.rollSize = 0
agent.sinks.hdfs-sink.hdfs.rollCount = 10

# ======================
# BINDING
# ======================
agent.sources.file-source.channels = mem-channel
agent.sinks.hdfs-sink.channel = mem-channel